{
  "name": "darkwrite",
  "version": "0.1.0-alpha.1",
  "description": "",
  "scripts": {
    "test:all": "pnpm -r test",
    "dev": "concurrently \" pnpm --filter common run watch \"  \" pnpm --filter app-desktop dev  \"",
    "build:win": "pnpm --filter common build && pnpm --filter app-desktop build:win",
    "build:linux": "pnpm --filter common build && pnpm --filter app-desktop build:linux",
    "build:mac": "pnpm --filter common build && pnpm --filter app-desktop build:mac"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
